# Specyfikacja Diagramów UML - Sieciowa Tablica Ogłoszeń
## Do ręcznego rysowania w Visual Paradigm

---

# 1. DIAGRAM KLAS

## Klasy pakietu `wspolne` (kolor: ŻÓŁTY)

### Klasa: OgloszenieDTO
```
┌─────────────────────────────┐
│      OgloszenieDTO          │
├─────────────────────────────┤
│ - id: int                   │
│ - tytul: String             │
│ - tresc: String             │
│ - daneKontaktowe: String    │
│ - kategoria: String         │
│ - idAutora: int             │
│ - autor: String             │
│ - dataDodania: LocalDateTime│
│ - wyswietlenia: int         │
│ - zgloszenia: int           │
├─────────────────────────────┤
│ + gettery/settery()         │
└─────────────────────────────┘
```
Implementuje: `Serializable`

### Klasa: UzytkownikDTO
```
┌─────────────────────────────┐
│      UzytkownikDTO          │
├─────────────────────────────┤
│ - id: int                   │
│ - login: String             │
│ - rola: String              │
├─────────────────────────────┤
│ + gettery/settery()         │
└─────────────────────────────┘
```
Implementuje: `Serializable`

### Klasa: FiltrDTO
```
┌─────────────────────────────┐
│        FiltrDTO             │
├─────────────────────────────┤
│ - kategoria: String         │
│ - autor: String             │
│ - tekst: String             │
│ - sortowanie: String        │
├─────────────────────────────┤
│ + gettery/settery()         │
└─────────────────────────────┘
```
Implementuje: `Serializable`

### Klasa: Zadanie
```
┌─────────────────────────────┐
│         Zadanie             │
├─────────────────────────────┤
│ - typ: TypZadania           │
│ - dane: Object              │
├─────────────────────────────┤
│ + getTyp(): TypZadania      │
│ + getDane(): Object         │
└─────────────────────────────┘
```
Implementuje: `Serializable`

### Klasa: Odpowiedz
```
┌─────────────────────────────┐
│        Odpowiedz            │
├─────────────────────────────┤
│ - status: StatusOdpowiedzi  │
│ - dane: Object              │
│ - wiadomosc: String         │
├─────────────────────────────┤
│ + getStatus()               │
│ + getDane()                 │
│ + getWiadomosc()            │
└─────────────────────────────┘
```
Implementuje: `Serializable`

### Enum: TypZadania (kolor: POMARAŃCZOWY)
```
┌─────────────────────────────┐
│     <<enumeration>>         │
│       TypZadania            │
├─────────────────────────────┤
│ ZALOGUJ                     │
│ ZAREJESTRUJ                 │
│ POBIERZ_OGLOSZENIA_FILTR    │
│ POBIERZ_KATEGORIE           │
│ DODAJ_OGLOSZENIE            │
│ EDYTUJ_OGLOSZENIE           │
│ USUN_OGLOSZENIE             │
│ ZGLOS_OGLOSZENIE            │
│ POBIERZ_SZCZEGOLY           │
│ POBIERZ_ZGLOSZONE           │
│ GENERUJ_RAPORT              │
└─────────────────────────────┘
```

### Enum: StatusOdpowiedzi (kolor: POMARAŃCZOWY)
```
┌─────────────────────────────┐
│     <<enumeration>>         │
│    StatusOdpowiedzi         │
├─────────────────────────────┤
│ OK                          │
│ BLAD                        │
│ ODSWIEZ                     │
└─────────────────────────────┘
```

---

## Klasy pakietu `serwer` (kolor: ZIELONY)

### Klasa: Serwer
```
┌─────────────────────────────┐
│         Serwer              │
├─────────────────────────────┤
│ - PORT: int = 5000          │
│ - klienci: List<ObslugaKlienta>│
│ - executor: ExecutorService │
├─────────────────────────────┤
│ + main(args): void          │
│ + start(): void             │
│ + dodajKlienta(k): void     │
│ + usunKlienta(k): void      │
│ + powiadomWszystkich(): void│
└─────────────────────────────┘
```

### Klasa: ObslugaKlienta
```
┌─────────────────────────────┐
│     ObslugaKlienta          │
├─────────────────────────────┤
│ - socket: Socket            │
│ - wejscie: ObjectInputStream│
│ - wyjscie: ObjectOutputStream│
│ - zalogowany: UzytkownikDTO │
├─────────────────────────────┤
│ + run(): void               │
│ - obslozZadanie(z): void    │
│ + wyslij(odp): void         │
└─────────────────────────────┘
```
Implementuje: `Runnable`

### Klasa: PolaczenieBazy (Singleton)
```
┌─────────────────────────────┐
│  <<Singleton>>              │
│    PolaczenieBazy           │
├─────────────────────────────┤
│ - instancja: PolaczenieBazy │
│ - polaczenie: Connection    │
├─────────────────────────────┤
│ + pobierzPolaczenie(): Connection│
│ + zamknijPolaczenie(): void │
└─────────────────────────────┘
```

### Klasa: OgloszenieDAO
```
┌─────────────────────────────┐
│      OgloszenieDAO          │
├─────────────────────────────┤
├─────────────────────────────┤
│ + pobierzZFiltrem(f): List  │
│ + dodaj(o, idAutora): int   │
│ + edytuj(o): void           │
│ + usun(id, idAutora): void  │
│ + zwiekszWyswietlenia(id): void│
│ + zglosOgloszenie(id): void │
│ + pobierzZgloszone(): List  │
│ + generujRaport(): String   │
└─────────────────────────────┘
```

### Klasa: UzytkownikDAO
```
┌─────────────────────────────┐
│      UzytkownikDAO          │
├─────────────────────────────┤
├─────────────────────────────┤
│ + zaloguj(login, haslo): UzytkownikDTO│
│ + zarejestruj(login, haslo): boolean│
└─────────────────────────────┘
```

### Klasa: Bezpieczenstwo
```
┌─────────────────────────────┐
│     Bezpieczenstwo          │
├─────────────────────────────┤
├─────────────────────────────┤
│ + hashuj(haslo): String     │
└─────────────────────────────┘
```

---

## Klasy pakietu `klient` (kolor: NIEBIESKI)

### Klasa: AplikacjaKlienta
```
┌─────────────────────────────┐
│    AplikacjaKlienta         │
├─────────────────────────────┤
│ - stage: Stage              │
├─────────────────────────────┤
│ + start(stage): void        │
│ + zaladujWidok(nazwa): void │
│ + main(args): void          │
└─────────────────────────────┘
```
Dziedziczy: `Application`

### Klasa: KlientSieciowy (Singleton)
```
┌─────────────────────────────┐
│  <<Singleton>>              │
│    KlientSieciowy           │
├─────────────────────────────┤
│ - instancja: KlientSieciowy │
│ - socket: Socket            │
│ - wejscie: ObjectInputStream│
│ - wyjscie: ObjectOutputStream│
├─────────────────────────────┤
│ + pobierzInstancje(): KlientSieciowy│
│ + wyslijISprawdz(z): Odpowiedz│
│ + nasluchuj(): void         │
└─────────────────────────────┘
```

### Klasa: Sesja
```
┌─────────────────────────────┐
│         Sesja               │
├─────────────────────────────┤
│ - zalogowany: UzytkownikDTO │
├─────────────────────────────┤
│ + getZalogowanyUzytkownik() │
│ + setZalogowanyUzytkownik() │
│ + wyloguj(): void           │
│ + czyAdmin(): boolean       │
└─────────────────────────────┘
```

### Klasa: KontrolerTablicy
```
┌─────────────────────────────┐
│    KontrolerTablicy         │
├─────────────────────────────┤
│ - kontenerOgloszen: FlowPane│
│ - filtrKategoria: ComboBox  │
│ - filtrSortowanie: ComboBox │
├─────────────────────────────┤
│ + initialize(): void        │
│ + odswiezListe(): void      │
│ + zastosujFiltr(): void     │
│ + pokazDialogDodawania(): void│
│ + pokazZgloszone(): void    │
│ + generujRaport(): void     │
└─────────────────────────────┘
```

---

## RELACJE MIĘDZY KLASAMI

### Dziedziczenie (strzałka z pustym trójkątem △):
- `AplikacjaKlienta` ──△──> `Application`

### Implementacja (przerywana strzałka z pustym trójkątem):
- `OgloszenieDTO` - - -△- -> `Serializable`
- `UzytkownikDTO` - - -△- -> `Serializable`
- `Zadanie` - - -△- -> `Serializable`
- `Odpowiedz` - - -△- -> `Serializable`
- `ObslugaKlienta` - - -△- -> `Runnable`

### Zależności (przerywana strzałka):
- `Serwer` - - -> `ObslugaKlienta` (tworzy)
- `ObslugaKlienta` - - -> `OgloszenieDAO` (używa)
- `ObslugaKlienta` - - -> `UzytkownikDAO` (używa)
- `KontrolerTablicy` - - -> `KlientSieciowy` (używa)
- `UzytkownikDAO` - - -> `Bezpieczenstwo` (używa)

### Agregacja (linia z pustym rombem ◇):
- `Zadanie` ◇──── `TypZadania`
- `Odpowiedz` ◇──── `StatusOdpowiedzi`

---

# 2. DIAGRAM PRZYPADKÓW UŻYCIA

## Aktorzy (ludziki):
1. **Gość** (Guest) - niezalogowany użytkownik
2. **Użytkownik** (User) - zalogowany użytkownik (dziedziczy po Gościu)
3. **Administrator** (Admin) - administrator systemu (dziedziczy po Użytkowniku)

## Przypadki użycia (elipsy - kolor NIEBIESKI):

### Dla Gościa:
- 🔵 **Przeglądaj ogłoszenia** (UC1)
- 🔵 **Filtruj ogłoszenia** (UC2) - <<extend>> UC1
- 🔵 **Sortuj ogłoszenia** (UC3) - <<extend>> UC1
- 🔵 **Zaloguj się** (UC4)
- 🔵 **Zarejestruj się** (UC5)

### Dla Użytkownika (dodatkowo):
- 🔵 **Dodaj ogłoszenie** (UC6)
- 🔵 **Edytuj własne ogłoszenie** (UC7)
- 🔵 **Usuń własne ogłoszenie** (UC8)
- 🔵 **Wyświetl szczegóły** (UC9) - <<extend>> UC1
- 🔵 **Zgłoś ogłoszenie** (UC10)
- 🔵 **Wyloguj się** (UC11)

### Dla Administratora (dodatkowo):
- 🔵 **Przeglądaj zgłoszone** (UC12)
- 🔵 **Usuń zgłoszone ogłoszenie** (UC13) - <<include>> UC12
- 🔵 **Generuj raport** (UC14)

---

## RELACJE (strzałki):

### Dziedziczenie aktorów:
```
Gość <|── Użytkownik <|── Administrator
```

### Include (strzałka przerywana z <<include>>):
- UC12 (Przeglądaj zgłoszone) --<<include>>--> UC13 (Usuń zgłoszone)

### Extend (strzałka przerywana z <<extend>>):
- UC2 (Filtruj) --<<extend>>--> UC1 (Przeglądaj)
- UC3 (Sortuj) --<<extend>>--> UC1 (Przeglądaj)
- UC9 (Szczegóły) --<<extend>>--> UC1 (Przeglądaj)

---

## UKŁAD DIAGRAMU:

```
                    ┌─────────────────────────────────────────────────┐
                    │         System: Tablica Ogłoszeń                │
                    │                                                 │
   ┌───┐            │   (UC5 Zarejestruj)    (UC4 Zaloguj)           │
   │ G │────────────┼─────────○─────────────────○                    │
   │ O │            │         │                 │                    │
   │ Ś │            │         │    <<extend>>   │                    │
   │ Ć │────────────┼─────────○ UC2 Filtruj ────┤                    │
   └─┬─┘            │         │    <<extend>>   │                    │
     │              │   ┌─────○ UC3 Sortuj ─────┤                    │
     │              │   │     │                 │                    │
     ▽              │   │     ○ UC1 Przeglądaj ○ UC9 Szczegóły      │
   ┌───┐            │   │     │    <<extend>>   │                    │
   │ U │────────────┼───┴─────○────────────────○                    │
   │ Ż │            │         │                                      │
   │ Y │────────────┼─────────○ UC6 Dodaj                           │
   │ T │────────────┼─────────○ UC7 Edytuj                          │
   │ K │────────────┼─────────○ UC8 Usuń własne                     │
   │ O │────────────┼─────────○ UC10 Zgłoś                          │
   │ W │────────────┼─────────○ UC11 Wyloguj                        │
   └─┬─┘            │                                                │
     │              │                                                │
     ▽              │                                                │
   ┌───┐            │   ┌─────○ UC12 Przeglądaj zgłoszone           │
   │ A │────────────┼───┤     │     <<include>>                     │
   │ D │            │   └─────○ UC13 Usuń zgłoszone                 │
   │ M │────────────┼─────────○ UC14 Generuj raport                 │
   │ I │            │                                                │
   │ N │            │                                                │
   └───┘            │                                                │
                    └─────────────────────────────────────────────────┘
```

---

# 3. KOLORY W VISUAL PARADIGM

| Element | Kolor | RGB |
|---------|-------|-----|
| Klasy DTO (wspolne) | Żółty | #FFFFCC |
| Klasy serwera | Zielony | #CCFFCC |
| Klasy klienta | Niebieski | #CCE5FF |
| Enumy | Pomarańczowy | #FFCC99 |
| Przypadki użycia | Niebieski | #66B2FF |
| Aktorzy | Czarny/biały | - |

---

**Użyj tego dokumentu jako przewodnika do rysowania w Visual Paradigm!**
